将“请求”封装成对象，以便使用不同的请求、队列或者日志来参数化其他对象。命令模式也支持撤销的操作
就像在做命令行操作,只是将命令语句封装成对象,然后调用执行命令
1、调用者（Invoker）：持有一个命令对象，并在某个时间点调用命令对象的execute()方法。

2、命令对象（Command）：持有一个接收者，和一个execute()方法，在execute中执行接收者的动作。

3、接收者（Receiver）：动作的执行者

**命令行模式 https://www.cnblogs.com/zyrblog/p/9252664.html**
